The command files in this folder only need to be used if you wish to use TradeBuild components in your own programs.

To register the TradeBuild components run registerdlls.bat

To de-register the TradeBuild components run unregisterdlls.bat

Note that you need administrator privileges to run these command files.

